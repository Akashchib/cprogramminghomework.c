#include<stdio.h>
void main()
{
   int age;
   printf("enter the age");
   scanf("%d",&age);
   if(age>=19 && age<=50)
   if(age>=18 && age<=25) 
   {
    printf("eligble for grade a job");
   }
   else
   {
    printf("eligble for grade b job");
   }
   else
   {
    printf("young or not eligble for job");
   }
   }
   
